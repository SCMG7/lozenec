[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma migrate resolve --rolled-back 20260310_init && npx prisma migrate deploy && node dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
